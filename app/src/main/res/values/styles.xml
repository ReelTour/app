<resources>
    <style name="GHButton">
        <item name="android:background">@drawable/ghost_button_bg</item>
        <item name="android:paddingTop">10dp</item>
        <item name="android:paddingBottom">10dp</item>
        <item name="android:paddingLeft">5dp</item>
        <item name="android:paddingRight">5dp</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:color">#FFFFFF</item>
        <item name="android:textAppearance">?android:attr/textAppearanceMedium</item>
        <item name="android:gravity">center</item>
    </style>
    <style name="GHDialog_title" parent="android:Widget.TextView">
        <item name="android:drawable">@drawable/gui_background</item>
        <item name="android:textSize">22dp</item>
        <item name="android:padding">10dp</item>
    </style>
    <style name="GHDialog" parent="@android:style/Theme.Dialog">
        <item name="android:textColor">#fff</item>
        <item name="android:color">#fff</item>
        <item name="android:windowTitleStyle">@style/GHDialog_title</item>
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- Customize your theme here. -->
        <item name="android:buttonStyle">@style/GHButton</item>
    </style>
    <color name="black">#FF000000</color>
</resources>
